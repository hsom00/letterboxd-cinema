# ---- Letterboxd Cinema settings (copy to .env, or let scripts/install.* write this for you) ----

# What the site calls itself (wordmark, tab title)
APP_NAME=Cinema

# Where things live on this machine. Forward slashes work on Windows too (D:/Media).
MEDIA_PATH=D:/Media
CONFIG_PATH=D:/Cinema/config

TZ=Europe/London
PUID=1000
PGID=1000

# Access. Either a public hostname you own (HTTPS is automatic) or ":80" for LAN-only.
SITE_ADDRESS=:80
# For a public hostname on Cloudflare DNS, also set:
#COMPOSE_PROFILES=public
#CLOUDFLARE_API_TOKEN=

# NVIDIA GPU transcoding for Jellyfin:
#COMPOSE_FILE=docker-compose.yml:docker-compose.nvidia.yml

# The account the cinema signs in with. Created in Jellyfin on first start if Jellyfin is fresh.
JELLYFIN_ADMIN_USER=
JELLYFIN_ADMIN_PASSWORD=

# Optional: your Letterboxd username — your watchlist becomes the library's shopping list.
LETTERBOXD_USER=
# Optional: Prowlarr indexer definitions to add on first start, comma-separated (e.g. yts,1337x). Leave empty to add your own.
DEFAULT_INDEXERS=
# Optional tuning
MAX_MB_PER_MINUTE=150
SEED_RATIO=1.0

# Service API keys. The install script generates these and writes them into each app's config
# before first start, so nothing needs copying by hand.
RADARR_API_KEY=
PROWLARR_API_KEY=
